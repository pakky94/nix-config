{ lib, config, ... }:
with lib;
{
/*
  config = mkIf config.pakky.programs.hyprland.enable {
    xdg = {
      configFile = {
        hypr_config = {
          enable = true;
          target = "hypr";
          source = ./hypr;
        };
        waybar_config = {
          enable = true;
          target = "waybar";
          source = ./waybar;
        };
      };
    };
  };
*/
}
